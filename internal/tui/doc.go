// Package tui holds the Bubble Tea `work` home model. Its domain-aware prompt
// wrappers and pickers have been migrated onto internal/present; the home is the
// last resident and is removed with the package once bare `work` adopts the
// static brand.
package tui
